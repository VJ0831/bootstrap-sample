var React = require('react');
var ReactDOM = require('react-dom');

var GridManager = require('./components/GridManager.jsx');

ReactDOM.render(<GridManager />, document.getElementById('mainPanel'));
